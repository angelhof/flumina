{application, flumina,
  [{vsn, "1.0.0"},
   {modules,
    [conf_gen,
     configuration,
     log_mod,
     mailbox,
     node,
     optimizer_centralized_greedy,
     optimizer_greedy,
     optimizer_sequential,
     opt_lib,
     producer,
     router,
     setup,
     testing,
     tracer,
     util]},
   {registered,
    [router,
     sink,
     num_messages_logger_process]}
]}.
